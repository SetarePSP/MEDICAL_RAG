# deploy-backend.yml — Auto-deploy backend to Google Cloud Run on push to main.
#
# Required GitHub Secrets (Repository → Settings → Secrets and variables → Actions):
#   GCP_PROJECT_ID, GCP_SA_KEY (full JSON of a service account key), GEMINI_API_KEY, …
#
# If auth fails with: "must specify exactly one of workload_identity_provider or credentials_json"
# → GCP_SA_KEY is missing, empty, or not visible to this workflow (use repository secrets, not only org without access).

name: Deploy Backend to Cloud Run

on:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - ".github/workflows/deploy-backend.yml"

env:
  REGION: us-central1
  SERVICE: medical-rag-api
  IMAGE: us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/medical-rag/api

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Fail if GCP_SA_KEY is missing
        if: ${{ secrets.GCP_SA_KEY == '' }}
        run: |
          echo "::error::GCP_SA_KEY is not set or is empty. In the repo: Settings → Secrets and variables → Actions → New repository secret. Name: GCP_SA_KEY. Value: paste the full JSON key from GCP (IAM → Service accounts → Keys → Add JSON key)."
          exit 1

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      - name: Build and push Docker image
        run: |
          docker build -t ${{ env.IMAGE }}:${{ github.sha }} -t ${{ env.IMAGE }}:latest backend/
          docker push ${{ env.IMAGE }}:${{ github.sha }}
          docker push ${{ env.IMAGE }}:latest

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE }} \
            --image ${{ env.IMAGE }}:${{ github.sha }} \
            --region ${{ env.REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --port 8080 \
            --memory 1Gi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 3 \
            --set-env-vars "\
          GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }},\
          GEMINI_CHAT_MODEL=gemini-2.5-flash,\
          SUPABASE_URL=${{ secrets.SUPABASE_URL }},\
          SUPABASE_SERVICE_ROLE_KEY=${{ secrets.SUPABASE_SERVICE_ROLE_KEY }},\
          GOOGLE_CLOUD_PROJECT=${{ secrets.GCP_PROJECT_ID }},\
          VERTEX_AI_LOCATION=us-central1,\
          GEMINI_EMBEDDING_BACKEND=vertex,\
          SENTRY_DSN=${{ secrets.SENTRY_DSN }},\
          LANGCHAIN_TRACING_V2=true,\
          LANGCHAIN_API_KEY=${{ secrets.LANGCHAIN_API_KEY }},\
          LANGCHAIN_PROJECT=medical-rag,\
          OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }},\
          OPENAI_TTS_SPEED=1.12,\
          ENVIRONMENT=production"

      - name: Show deployed URL
        run: |
          gcloud run services describe ${{ env.SERVICE }} \
            --region ${{ env.REGION }} \
            --format "value(status.url)"
